%%{init: {'theme': 'neutral', 'themeVariables': { 'background': 'transparent' }}}%%
flowchart TD
    OSM[".osm / .pbf File"] --> Handler[MapHandler: Libosmium]
    
    subgraph Parsing_Logic ["Data Extraction Layer"]
        direction TB
        
        %% Node Logic
        Handler -- "Nodes" --> StoreNodes[Filter Coordinates]
        
        %% Way Logic (Stacked below)
        Handler -- "Ways" --> Filter["Filter: 'highway' tags"]
        Filter -- "Metadata" --> Meta[Extract Speed, Lanes, One-Way]
    end
    
    %% Convergence
    StoreNodes --> Graph[(Directed Graph State)]
    Meta --> Graph